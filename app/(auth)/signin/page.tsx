import SignIn from "./SignIn"; // Only import the client component

export const metadata = {
  title: "Sign In - Cloud Project",
  description: "Page description",
};

export default function SignInPage() {
  return <SignIn />;
}
